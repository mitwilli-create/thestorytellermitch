media/
tools/poster-candidates/
.DS_Store
tools/still-candidates/

.env
.dev.vars
node_modules
__pycache__/
tools/.kb-corpus.json
tools/.kb-eval-report.json

# wrangler local state / OAuth account cache (machine-local, not for a public repo)
.wrangler/

# cloud-sync conflict twins (iCloud "name 2.ext" duplicates): never track, never ship
* 2.*
* 2

# raw TTS takes: the paid artifact, cached so a loudness retune is free
assets/audio/narration/.cache/
